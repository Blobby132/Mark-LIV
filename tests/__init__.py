"""
Tests for the Mark-LIV safety primitives.

Standard-library `unittest` on purpose: the repository has no test dependency
today, and adding pytest to run six files of assertions would be a dependency
bought with someone else's install time. Run them with

    python -m unittest discover -s tests -v

from the repository root. They also run unchanged under pytest for anyone who
already has it.
"""
